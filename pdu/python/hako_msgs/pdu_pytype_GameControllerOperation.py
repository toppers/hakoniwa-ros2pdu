from typing import List

# dependencies for the generated Python class


# class definition for the ROS message
class GameControllerOperation:
    """
    ROS message: hako_msgs/GameControllerOperation
    Generated by hakoniwa-ros2pdu.
    """

    axis: List[float]
    button: List[bool]

    def __init__(self):
        self.axis = []
        self.button = []

    def __str__(self):
        return f"GameControllerOperation(" + ", ".join([
            f"axis={self.axis}"
            f"button={self.button}"
        ]) + ")"

    def __repr__(self):
        return self.__str__()

    def to_dict(self):
        """
        Convert the object to a dictionary.
        """
        d = {}
        # handle field 'axis'
        field_val = self.axis
        if isinstance(field_val, bytearray):
            d['axis'] = list(field_val)
        elif hasattr(field_val, 'to_dict'):
            d['axis'] = field_val.to_dict()
        elif isinstance(field_val, list):
            d['axis'] = [item.to_dict() if hasattr(item, 'to_dict') else item for item in field_val]
        else:
            d['axis'] = field_val
        # handle field 'button'
        field_val = self.button
        if isinstance(field_val, bytearray):
            d['button'] = list(field_val)
        elif hasattr(field_val, 'to_dict'):
            d['button'] = field_val.to_dict()
        elif isinstance(field_val, list):
            d['button'] = [item.to_dict() if hasattr(item, 'to_dict') else item for item in field_val]
        else:
            d['button'] = field_val
        return d

    @classmethod
    def from_dict(cls, d: dict):
        """
        Create an object from a dictionary.
        """
        obj = cls()
        # handle field 'axis'
        if 'axis' in d:
            field_type = cls.__annotations__.get('axis')
            value = d['axis']
            
            if field_type is bytearray:
                obj.axis = bytearray(value)
            elif hasattr(field_type, '__origin__') and field_type.__origin__ is list:
                list_item_type = field_type.__args__[0]
                if hasattr(list_item_type, 'from_dict'):
                    obj.axis = [list_item_type.from_dict(item) for item in value]
                else:
                    obj.axis = value
            elif hasattr(field_type, 'from_dict'):
                obj.axis = field_type.from_dict(value)
            else:
                obj.axis = value
        # handle field 'button'
        if 'button' in d:
            field_type = cls.__annotations__.get('button')
            value = d['button']
            
            if field_type is bytearray:
                obj.button = bytearray(value)
            elif hasattr(field_type, '__origin__') and field_type.__origin__ is list:
                list_item_type = field_type.__args__[0]
                if hasattr(list_item_type, 'from_dict'):
                    obj.button = [list_item_type.from_dict(item) for item in value]
                else:
                    obj.button = value
            elif hasattr(field_type, 'from_dict'):
                obj.button = field_type.from_dict(value)
            else:
                obj.button = value
        return obj

    def to_json(self, indent=2):
        """
        Convert the object to a JSON string.
        """
        import json
        return json.dumps(self.to_dict(), indent=indent)

    @classmethod
    def from_json(cls, json_str: str):
        """
        Create an object from a JSON string.
        """
        import json
        return cls.from_dict(json.loads(json_str))
