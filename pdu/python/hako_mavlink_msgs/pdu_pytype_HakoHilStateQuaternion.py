from typing import List

# dependencies for the generated Python class


# class definition for the ROS message
class HakoHilStateQuaternion:
    """
    ROS message: hako_mavlink_msgs/HakoHilStateQuaternion
    Generated by hakoniwa-ros2pdu.
    """
    
    def __init__(self):
        self.time_usec: int = 0
        self.attitude_quaternion: List[float] = []
        self.rollspeed: float = 0.0
        self.pitchspeed: float = 0.0
        self.yawspeed: float = 0.0
        self.lat: int = 0
        self.lon: int = 0
        self.alt: int = 0
        self.vx: int = 0
        self.vy: int = 0
        self.vz: int = 0
        self.ind_airspeed: int = 0
        self.true_airspeed: int = 0
        self.xacc: int = 0
        self.yacc: int = 0
        self.zacc: int = 0

    def __str__(self):
        return f"HakoHilStateQuaternion(" + ", ".join([
            f"time_usec={self.time_usec}"
            f"attitude_quaternion={self.attitude_quaternion}"
            f"rollspeed={self.rollspeed}"
            f"pitchspeed={self.pitchspeed}"
            f"yawspeed={self.yawspeed}"
            f"lat={self.lat}"
            f"lon={self.lon}"
            f"alt={self.alt}"
            f"vx={self.vx}"
            f"vy={self.vy}"
            f"vz={self.vz}"
            f"ind_airspeed={self.ind_airspeed}"
            f"true_airspeed={self.true_airspeed}"
            f"xacc={self.xacc}"
            f"yacc={self.yacc}"
            f"zacc={self.zacc}"
        ]) + ")"

    def __repr__(self):
        return self.__str__()
