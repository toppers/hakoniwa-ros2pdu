

/**
 * ROS message: ev3_msgs/Ev3PduTouchSensor
 * Generated by hakoniwa-ros2pdu.
 */
export class Ev3PduTouchSensor {
/** @type { number } */
    value;


    constructor() {
        this.value = 0;
    }

    /**
     * @returns {object}
     */
    toDict() {
        const d = {};
        {
            // handle field 'value'
            const field_val = this.value;
            if (typeof field_val?.toDict === 'function') {
                d['value'] = field_val.toDict();
            } else if (Array.isArray(field_val)) {
                d['value'] = field_val.map(item => typeof item?.toDict === 'function' ? item.toDict() : item);
            } else {
                d['value'] = field_val;
            }
        }
        return d;
    }

    /**
     * @param {object} d
     * @returns { Ev3PduTouchSensor }
     */
    static fromDict(d) {
        const obj = new Ev3PduTouchSensor();
        if (d.hasOwnProperty('value')) {
            obj.value = d.value;
        }
        return obj;
    }

    /**
     * @returns {string}
     */
    toJSON(indent = 2) {
        return JSON.stringify(this.toDict(), null, indent);
    }

    /**
     * @param {string} json_str
     * @returns { Ev3PduTouchSensor }
     */
    static fromJSON(json_str) {
        return this.fromDict(JSON.parse(json_str));
    }
}
