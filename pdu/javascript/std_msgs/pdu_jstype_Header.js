import { Time } from '../builtin_interfaces/pdu_jstype_Time.js';


/**
 * ROS message: std_msgs/Header
 * Generated by hakoniwa-ros2pdu.
 */
export class Header {
/** @type { Time } */
    stamp;
/** @type { string } */
    frame_id;


    constructor() {
        this.stamp = new Time();
        this.frame_id = "";
    }

    /**
     * @returns {object}
     */
    toDict() {
        const d = {};
        {
            // handle field 'stamp'
            const field_val = this.stamp;
            if (typeof field_val?.toDict === 'function') {
                d['stamp'] = field_val.toDict();
            } else if (Array.isArray(field_val)) {
                d['stamp'] = field_val.map(item => typeof item?.toDict === 'function' ? item.toDict() : item);
            } else {
                d['stamp'] = field_val;
            }
        }
        {
            // handle field 'frame_id'
            const field_val = this.frame_id;
            if (typeof field_val?.toDict === 'function') {
                d['frame_id'] = field_val.toDict();
            } else if (Array.isArray(field_val)) {
                d['frame_id'] = field_val.map(item => typeof item?.toDict === 'function' ? item.toDict() : item);
            } else {
                d['frame_id'] = field_val;
            }
        }
        return d;
    }

    /**
     * @param {object} d
     * @returns { Header }
     */
    static fromDict(d) {
        const obj = new Header();
        if (d.hasOwnProperty('stamp')) {
            const field_class = Time;
            if (d.stamp) {
                obj.stamp = field_class.fromDict(d.stamp);
            }
        }
        if (d.hasOwnProperty('frame_id')) {
            obj.frame_id = d.frame_id;
        }
        return obj;
    }

    /**
     * @returns {string}
     */
    toJSON(indent = 2) {
        return JSON.stringify(this.toDict(), null, indent);
    }

    /**
     * @param {string} json_str
     * @returns { Header }
     */
    static fromJSON(json_str) {
        return this.fromDict(JSON.parse(json_str));
    }
}
