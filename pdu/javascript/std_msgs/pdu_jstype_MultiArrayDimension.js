

/**
 * ROS message: std_msgs/MultiArrayDimension
 * Generated by hakoniwa-ros2pdu.
 */
export class MultiArrayDimension {
/** @type { string } */
    label;
/** @type { number } */
    size;
/** @type { number } */
    stride;


    constructor() {
        this.label = "";
        this.size = 0;
        this.stride = 0;
    }

    /**
     * @returns {object}
     */
    toDict() {
        const d = {};
        {
            // handle field 'label'
            const field_val = this.label;
            if (typeof field_val?.toDict === 'function') {
                d['label'] = field_val.toDict();
            } else if (Array.isArray(field_val)) {
                d['label'] = field_val.map(item => typeof item?.toDict === 'function' ? item.toDict() : item);
            } else {
                d['label'] = field_val;
            }
        }
        {
            // handle field 'size'
            const field_val = this.size;
            if (typeof field_val?.toDict === 'function') {
                d['size'] = field_val.toDict();
            } else if (Array.isArray(field_val)) {
                d['size'] = field_val.map(item => typeof item?.toDict === 'function' ? item.toDict() : item);
            } else {
                d['size'] = field_val;
            }
        }
        {
            // handle field 'stride'
            const field_val = this.stride;
            if (typeof field_val?.toDict === 'function') {
                d['stride'] = field_val.toDict();
            } else if (Array.isArray(field_val)) {
                d['stride'] = field_val.map(item => typeof item?.toDict === 'function' ? item.toDict() : item);
            } else {
                d['stride'] = field_val;
            }
        }
        return d;
    }

    /**
     * @param {object} d
     * @returns { MultiArrayDimension }
     */
    static fromDict(d) {
        const obj = new MultiArrayDimension();
        if (d.hasOwnProperty('label')) {
            obj.label = d.label;
        }
        if (d.hasOwnProperty('size')) {
            obj.size = d.size;
        }
        if (d.hasOwnProperty('stride')) {
            obj.stride = d.stride;
        }
        return obj;
    }

    /**
     * @returns {string}
     */
    toJSON(indent = 2) {
        return JSON.stringify(this.toDict(), null, indent);
    }

    /**
     * @param {string} json_str
     * @returns { MultiArrayDimension }
     */
    static fromJSON(json_str) {
        return this.fromDict(JSON.parse(json_str));
    }
}
