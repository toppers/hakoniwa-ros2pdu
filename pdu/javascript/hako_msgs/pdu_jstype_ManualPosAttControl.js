import { Twist } from '../geometry_msgs/pdu_jstype_Twist.js';
import { Vector3 } from '../geometry_msgs/pdu_jstype_Vector3.js';


/**
 * ROS message: hako_msgs/ManualPosAttControl
 * Generated by hakoniwa-ros2pdu.
 */
export class ManualPosAttControl {
/** @type { boolean } */
    do_operation;
/** @type { Twist } */
    posatt;


    constructor() {
        this.do_operation = false;
        this.posatt = null;
    }

    /**
     * @returns {object}
     */
    toDict() {
        const d = {};
        {
            // handle field 'do_operation'
            const field_val = this.do_operation;
            if (typeof field_val?.toDict === 'function') {
                d['do_operation'] = field_val.toDict();
            } else if (Array.isArray(field_val)) {
                d['do_operation'] = field_val.map(item => typeof item?.toDict === 'function' ? item.toDict() : item);
            } else {
                d['do_operation'] = field_val;
            }
        }
        {
            // handle field 'posatt'
            const field_val = this.posatt;
            if (typeof field_val?.toDict === 'function') {
                d['posatt'] = field_val.toDict();
            } else if (Array.isArray(field_val)) {
                d['posatt'] = field_val.map(item => typeof item?.toDict === 'function' ? item.toDict() : item);
            } else {
                d['posatt'] = field_val;
            }
        }
        return d;
    }

    /**
     * @param {object} d
     * @returns { ManualPosAttControl }
     */
    static fromDict(d) {
        const obj = new ManualPosAttControl();
        if (d.hasOwnProperty('do_operation')) {
            obj.do_operation = d.do_operation;
        }
        if (d.hasOwnProperty('posatt')) {
            const field_class = Twist;
            if (d.posatt) {
                obj.posatt = field_class.fromDict(d.posatt);
            }
        }
        return obj;
    }

    /**
     * @returns {string}
     */
    toJSON(indent = 2) {
        return JSON.stringify(this.toDict(), null, indent);
    }

    /**
     * @param {string} json_str
     * @returns { ManualPosAttControl }
     */
    static fromJSON(json_str) {
        return this.fromDict(JSON.parse(json_str));
    }
}
