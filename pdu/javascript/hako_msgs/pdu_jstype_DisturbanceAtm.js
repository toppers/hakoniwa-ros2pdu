

/**
 * ROS message: hako_msgs/DisturbanceAtm
 * Generated by hakoniwa-ros2pdu.
 */
export class DisturbanceAtm {
/** @type { number } */
    sea_level_atm;


    constructor() {
        this.sea_level_atm = 0.0;
    }

    /**
     * @returns {object}
     */
    toDict() {
        const d = {};
        {
            // handle field 'sea_level_atm'
            const field_val = this.sea_level_atm;
            if (typeof field_val?.toDict === 'function') {
                d['sea_level_atm'] = field_val.toDict();
            } else if (Array.isArray(field_val)) {
                d['sea_level_atm'] = field_val.map(item => typeof item?.toDict === 'function' ? item.toDict() : item);
            } else {
                d['sea_level_atm'] = field_val;
            }
        }
        return d;
    }

    /**
     * @param {object} d
     * @returns { DisturbanceAtm }
     */
    static fromDict(d) {
        const obj = new DisturbanceAtm();
        if (d.hasOwnProperty('sea_level_atm')) {
            obj.sea_level_atm = d.sea_level_atm;
        }
        return obj;
    }

    /**
     * @returns {string}
     */
    toJSON(indent = 2) {
        return JSON.stringify(this.toDict(), null, indent);
    }

    /**
     * @param {string} json_str
     * @returns { DisturbanceAtm }
     */
    static fromJSON(json_str) {
        return this.fromDict(JSON.parse(json_str));
    }
}
