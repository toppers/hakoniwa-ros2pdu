import { Twist } from '../geometry_msgs/pdu_jstype_Twist.js';
import { Vector3 } from '../geometry_msgs/pdu_jstype_Vector3.js';


/**
 * ROS message: hako_msgs/ShareObjectOwner
 * Generated by hakoniwa-ros2pdu.
 */
export class ShareObjectOwner {
/** @type { string } */
    object_name;
/** @type { number } */
    owner_id;
/** @type { bigint } */
    last_update;
/** @type { Twist } */
    pos;


    constructor() {
        this.object_name = "";
        this.owner_id = 0;
        this.last_update = 0n;
        this.pos = new Twist();
    }

    /**
     * @returns {object}
     */
    toDict() {
        const d = {};
        {
            // handle field 'object_name'
            const field_val = this.object_name;
            if (typeof field_val?.toDict === 'function') {
                d['object_name'] = field_val.toDict();
            } else if (Array.isArray(field_val)) {
                d['object_name'] = field_val.map(item => typeof item?.toDict === 'function' ? item.toDict() : item);
            } else {
                d['object_name'] = field_val;
            }
        }
        {
            // handle field 'owner_id'
            const field_val = this.owner_id;
            if (typeof field_val?.toDict === 'function') {
                d['owner_id'] = field_val.toDict();
            } else if (Array.isArray(field_val)) {
                d['owner_id'] = field_val.map(item => typeof item?.toDict === 'function' ? item.toDict() : item);
            } else {
                d['owner_id'] = field_val;
            }
        }
        {
            // handle field 'last_update'
            const field_val = this.last_update;
            if (typeof field_val?.toDict === 'function') {
                d['last_update'] = field_val.toDict();
            } else if (Array.isArray(field_val)) {
                d['last_update'] = field_val.map(item => typeof item?.toDict === 'function' ? item.toDict() : item);
            } else {
                d['last_update'] = field_val;
            }
        }
        {
            // handle field 'pos'
            const field_val = this.pos;
            if (typeof field_val?.toDict === 'function') {
                d['pos'] = field_val.toDict();
            } else if (Array.isArray(field_val)) {
                d['pos'] = field_val.map(item => typeof item?.toDict === 'function' ? item.toDict() : item);
            } else {
                d['pos'] = field_val;
            }
        }
        return d;
    }

    /**
     * @param {object} d
     * @returns { ShareObjectOwner }
     */
    static fromDict(d) {
        const obj = new ShareObjectOwner();
        if (d.hasOwnProperty('object_name')) {
            obj.object_name = d.object_name;
        }
        if (d.hasOwnProperty('owner_id')) {
            obj.owner_id = d.owner_id;
        }
        if (d.hasOwnProperty('last_update')) {
            obj.last_update = d.last_update;
        }
        if (d.hasOwnProperty('pos')) {
            const field_class = Twist;
            if (d.pos) {
                obj.pos = field_class.fromDict(d.pos);
            }
        }
        return obj;
    }

    /**
     * @returns {string}
     */
    toJSON(indent = 2) {
        return JSON.stringify(this.toDict(), null, indent);
    }

    /**
     * @param {string} json_str
     * @returns { ShareObjectOwner }
     */
    static fromJSON(json_str) {
        return this.fromDict(JSON.parse(json_str));
    }
}
