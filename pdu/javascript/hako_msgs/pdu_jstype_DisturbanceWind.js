import { Vector3 } from '../geometry_msgs/pdu_jstype_Vector3.js';


/**
 * ROS message: hako_msgs/DisturbanceWind
 * Generated by hakoniwa-ros2pdu.
 */
export class DisturbanceWind {
/** @type { Vector3 } */
    value;


    constructor() {
        this.value = new Vector3();
    }

    /**
     * @returns {object}
     */
    toDict() {
        const d = {};
        {
            // handle field 'value'
            const field_val = this.value;
            if (typeof field_val?.toDict === 'function') {
                d['value'] = field_val.toDict();
            } else if (Array.isArray(field_val)) {
                d['value'] = field_val.map(item => typeof item?.toDict === 'function' ? item.toDict() : item);
            } else {
                d['value'] = field_val;
            }
        }
        return d;
    }

    /**
     * @param {object} d
     * @returns { DisturbanceWind }
     */
    static fromDict(d) {
        const obj = new DisturbanceWind();
        if (d.hasOwnProperty('value')) {
            const field_class = Vector3;
            if (d.value) {
                obj.value = field_class.fromDict(d.value);
            }
        }
        return obj;
    }

    /**
     * @returns {string}
     */
    toJSON(indent = 2) {
        return JSON.stringify(this.toDict(), null, indent);
    }

    /**
     * @param {string} json_str
     * @returns { DisturbanceWind }
     */
    static fromJSON(json_str) {
        return this.fromDict(JSON.parse(json_str));
    }
}
