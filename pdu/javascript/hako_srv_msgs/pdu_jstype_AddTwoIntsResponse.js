

/**
 * ROS message: hako_srv_msgs/AddTwoIntsResponse
 * Generated by hakoniwa-ros2pdu.
 */
export class AddTwoIntsResponse {
/** @type { bigint } */
    sum;


    constructor() {
        this.sum = 0n;
    }

    /**
     * @returns {object}
     */
    toDict() {
        const d = {};
        {
            // handle field 'sum'
            const field_val = this.sum;
            if (typeof field_val?.toDict === 'function') {
                d['sum'] = field_val.toDict();
            } else if (Array.isArray(field_val)) {
                d['sum'] = field_val.map(item => typeof item?.toDict === 'function' ? item.toDict() : item);
            } else {
                d['sum'] = field_val;
            }
        }
        return d;
    }

    /**
     * @param {object} d
     * @returns { AddTwoIntsResponse }
     */
    static fromDict(d) {
        const obj = new AddTwoIntsResponse();
        if (d.hasOwnProperty('sum')) {
            obj.sum = d.sum;
        }
        return obj;
    }

    /**
     * @returns {string}
     */
    toJSON(indent = 2) {
        return JSON.stringify(this.toDict(), null, indent);
    }

    /**
     * @param {string} json_str
     * @returns { AddTwoIntsResponse }
     */
    static fromJSON(json_str) {
        return this.fromDict(JSON.parse(json_str));
    }
}
