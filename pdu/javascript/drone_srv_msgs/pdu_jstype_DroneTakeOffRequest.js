

/**
 * ROS message: drone_srv_msgs/DroneTakeOffRequest
 * Generated by hakoniwa-ros2pdu.
 */
export class DroneTakeOffRequest {
/** @type { number } */
    alt_m;
/** @type { string } */
    drone_name;


    constructor() {
        this.alt_m = 0.0;
        this.drone_name = "";
    }

    /**
     * @returns {object}
     */
    toDict() {
        const d = {};
        {
            // handle field 'alt_m'
            const field_val = this.alt_m;
            if (typeof field_val?.toDict === 'function') {
                d['alt_m'] = field_val.toDict();
            } else if (Array.isArray(field_val)) {
                d['alt_m'] = field_val.map(item => typeof item?.toDict === 'function' ? item.toDict() : item);
            } else {
                d['alt_m'] = field_val;
            }
        }
        {
            // handle field 'drone_name'
            const field_val = this.drone_name;
            if (typeof field_val?.toDict === 'function') {
                d['drone_name'] = field_val.toDict();
            } else if (Array.isArray(field_val)) {
                d['drone_name'] = field_val.map(item => typeof item?.toDict === 'function' ? item.toDict() : item);
            } else {
                d['drone_name'] = field_val;
            }
        }
        return d;
    }

    /**
     * @param {object} d
     * @returns { DroneTakeOffRequest }
     */
    static fromDict(d) {
        const obj = new DroneTakeOffRequest();
        if (d.hasOwnProperty('alt_m')) {
            obj.alt_m = d.alt_m;
        }
        if (d.hasOwnProperty('drone_name')) {
            obj.drone_name = d.drone_name;
        }
        return obj;
    }

    /**
     * @returns {string}
     */
    toJSON(indent = 2) {
        return JSON.stringify(this.toDict(), null, indent);
    }

    /**
     * @param {string} json_str
     * @returns { DroneTakeOffRequest }
     */
    static fromJSON(json_str) {
        return this.fromDict(JSON.parse(json_str));
    }
}
