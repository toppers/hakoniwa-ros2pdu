

/**
 * ROS message: drone_srv_msgs/DroneSetReadyResponse
 * Generated by hakoniwa-ros2pdu.
 */
export class DroneSetReadyResponse {
/** @type { boolean } */
    ok;
/** @type { string } */
    message;


    constructor() {
        this.ok = false;
        this.message = "";
    }

    /**
     * @returns {object}
     */
    toDict() {
        const d = {};
        {
            // handle field 'ok'
            const field_val = this.ok;
            if (typeof field_val?.toDict === 'function') {
                d['ok'] = field_val.toDict();
            } else if (Array.isArray(field_val)) {
                d['ok'] = field_val.map(item => typeof item?.toDict === 'function' ? item.toDict() : item);
            } else {
                d['ok'] = field_val;
            }
        }
        {
            // handle field 'message'
            const field_val = this.message;
            if (typeof field_val?.toDict === 'function') {
                d['message'] = field_val.toDict();
            } else if (Array.isArray(field_val)) {
                d['message'] = field_val.map(item => typeof item?.toDict === 'function' ? item.toDict() : item);
            } else {
                d['message'] = field_val;
            }
        }
        return d;
    }

    /**
     * @param {object} d
     * @returns { DroneSetReadyResponse }
     */
    static fromDict(d) {
        const obj = new DroneSetReadyResponse();
        if (d.hasOwnProperty('ok')) {
            obj.ok = d.ok;
        }
        if (d.hasOwnProperty('message')) {
            obj.message = d.message;
        }
        return obj;
    }

    /**
     * @returns {string}
     */
    toJSON(indent = 2) {
        return JSON.stringify(this.toDict(), null, indent);
    }

    /**
     * @param {string} json_str
     * @returns { DroneSetReadyResponse }
     */
    static fromJSON(json_str) {
        return this.fromDict(JSON.parse(json_str));
    }
}
